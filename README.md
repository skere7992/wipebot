# Rust Wipe Announcer Bot

Discord bot for managing Rust server wipe announcements.

## Setup
1. Copy `config.template.json` to `config.json`
2. Fill in your actual values in `config.json`
3. Run the bot
